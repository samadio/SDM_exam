package dotsandboxes;

import gamesuite.board.BoardManager;

public class DotsAndBoxesBoard extends BoardManager {
    public DotsAndBoxesBoard(Integer rows, Integer cols) {
        super(rows, cols);
    }
}
