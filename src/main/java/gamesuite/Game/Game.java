package gamesuite.Game;

import gamesuite.board.Board;
import gamesuite.status.GameScore;

public class Game {
/*    private Board board;
    private MoveValidator validator;
    private GameScore status;

    public Game(Board board, MoveValidator validator,GameStatus status){
        this.board=board;
        this.validator=validator;
        this.status=status;
    }

    public void applyMove(Move currentMove) throws InvalidMoveException {
        if(validator.isNotValidMove(currentMove)){
            throw new InvalidMoveException("The move selected (to be printed) is not valid");
        }
        board.applyMove(currentMove);
        status.update(currentMove);
    }

        public boolean notEnded();


 */
}
